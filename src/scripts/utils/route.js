export class Route {
  constructor (authority, logged, icon, name, path) {
    this.authority = authority
    this.logged = logged
    this.icon = icon
    this.name = name
    this.path = path
  }
}
